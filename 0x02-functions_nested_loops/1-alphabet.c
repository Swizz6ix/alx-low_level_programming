#include <stdio.h>

/**
 * main- prints the alphabets
 *
 * Return: returns zero if it is successful
 *
 * 
 */

 
 int main (void)
{
	char i;

	for (i = 'a'; i < 'z'; i++)
	{
		_putchar(i);
	}
	_putchar(i);

	return (0);

}
