#include <stdio>
#include <stdlib.h>


int main(void)
{
	int num = 48;

	while (num < 58)
	{
		purchar(num);
		if (num < 57)
		{
			putchar(',');
			putchar(' ');
		}
	}
		num ++
	putchar('\n')
	return (0)
