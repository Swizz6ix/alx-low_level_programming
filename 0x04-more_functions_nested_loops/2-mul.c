#include "main.h"

/**
 * mul - prints the multiple of two numbers
 *
 * @b: one of the integers
 * @a: one of the integers
 *
 * Return: rerurns zero on success
 *
 */

int mul(int a, int b)
{
	return (a * b);
}
